@model IEnumerable<AsistenciaGR.Models.Asistencia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AsFecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AsPresente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AsJustificacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AsFecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AsPresente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AsJustificacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
