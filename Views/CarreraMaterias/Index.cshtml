@model IEnumerable<AsistenciaGR.Models.CarreraMateria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.CaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Carrera)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Materia)
            </th>
@*             <th>
                @Html.DisplayNameFor(model => model.Inscripciones)
            </th> *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.CaId)
            </td>
            <td>
                @Html.DisplayFor(model => item.Carrera)
            </td>
            <td>
                @Html.DisplayFor(model => item.MaId)
            </td>
            <td>
                @Html.DisplayFor(model => item.Materia)
            </td>
@*             <td>
                @Html.DisplayFor(model => item.Inscripciones)
            </td> *@
            <td>
                <a asp-action="Edit" asp-route-id="@item.CaMaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CaMaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CaMaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
