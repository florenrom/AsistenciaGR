@model IEnumerable<AsistenciaGR.Models.Materias>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaDenominacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaModalidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaCantHoras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaDenominacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaModalidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaCantHoras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
